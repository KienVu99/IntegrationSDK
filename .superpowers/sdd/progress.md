# IntegrationSDK Implementation Progress Ledger

Plan: docs/superpowers/plans/2026-07-23-integration-sdk-implementation.md
Branch: main (user consented to direct work on main)

Task 1: complete (commits 25241b7..ffbe962, review clean)
Task 2: complete (commits ffbe962..007a28f, review clean; includes testables config fix)
Task 3: complete (commits 007a28f..d0abdd5, review clean)
Task 4: complete (commits d0abdd5..0d8b2e7, review approved; 1 fix round for missing SafeInvoke exception guard on AdManager provider calls; 1 open Minor: ThrowingAdProvider only exercised via ShowInterstitial test, not the other 3 methods)
Task 5: complete (commit 0d8b2e7..0f4ceb5, review approved; Minor: LevelTracker.NowProvider set to null in TearDown (latent NRE risk if reused without reassigning))
NOTE (pending user follow-up): Tracking.md:64 lists level_start params as `level, time_played`, but Task 5 LevelTracker.Start only sends `{level}` (matches the plan/brief verbatim, not Tracking.md). User asked to continue and will check/fix this later themselves — do not "fix" it unprompted in a later task.
Task 6: complete (commits 0f4ceb5..95a5b2a, review approved; Minor: Object == dedup relies on Unity's overloaded equality, not literal reference equality (safe for this use case))
Fix: committed missing .meta files for Task 5/6 script files (commit c5d07db) that had been left untracked by prior implementer subagents
Task 7: complete (commit c5d07db..12b2a46, review clean)
Task 8: complete (commit 12b2a46..fa38dfb, review clean; no automated test per brief). PENDING: manual GUI verification (Tools > Integration SDK > Settings — save dialog, field fill, Player Settings > Preloaded Assets check, reopen-no-duplicate-dialog) not yet done by a human in the Unity Editor.
Task 9: complete (commit fa38dfb..fcbe96c, review clean; no automated test per brief, Bootstrapper's RuntimeInitializeOnLoadMethod verified only via clean compile + no regressions)
Core package (Tasks 1-9) fully implemented.
Task 10 (Ads.AppLovin): BLOCKED — AppLovin MAX SDK (MaxSdk/MaxSdkCallbacks) not installed in this project; requires manual AppLovin Integration Manager GUI step by the user. Not attempted, no files created.
Task 11 (Tracking.AppsFlyer): complete (commit c6dc1f3, review approved; Minor: AppsFlyerBackend.LogEvent stringifies numeric param values via culture-dependent ToString(), not invariant - matches plan brief as-is, flagged for future pass only).
  - History (resolved, kept for reference):
  - First attempt was BLOCKED on a red herring: headless git clone failed with "could not read Username for 'https://github.com'". Root cause was NOT missing credentials — the plan's brief has a URL TYPO: `https://github.com/AppsFlyerSDK/appsflyer-plugin.git#upm` should be `https://github.com/AppsFlyerSDK/appsflyer-unity-plugin.git#upm` (missing "unity"). Confirmed correct repo via WebSearch.
  - User manually added the corrected URL to Packages/manifest.json themselves (both appsflyer-unity-plugin and com.google.external-dependency-manager) and reopened the real Unity Editor. Package resolved successfully: `Packages/packages-lock.json` has `appsflyer-unity-plugin` entry, `Library/PackageCache/appsflyer-unity-plugin@d26fcb5d8c` contains `AppsFlyer.cs`, `AFAdRevenueData.cs`, and a `MediationNetwork` enum (in AppsFlyerAndroid.cs/AppsFlyeriOS.cs). This part is DONE and confirmed working — do not redo it.
  - Dispatched implementer subagent to write `Tracking.AppsFlyer/` package (package.json, asmdef, AppsFlyerBackend.cs, AppsFlyerBackendBootstrap.cs, README.md) with the corrected URL. Files were created (uncommitted, no .meta issues reported) but the subagent could not verify compile via batchmode because a real Unity Editor GUI had the project open/locked (`Temp/UnityLockfile` present) — reported DONE_WITH_CONCERNS.
  - Current files on disk (uncommitted): `Tracking.AppsFlyer/package.json`, `Tracking.AppsFlyer/Runtime/IntegrationSDK.Tracking.AppsFlyer.Runtime.asmdef` (already has `"AppsFlyer"` added to its `references` array — this looked like the right fix for CS0234/CS0246/CS0400/CS0103 errors seen in Editor.log for missing AppsFlyer/AFAdRevenueData/MediationNetwork types), `Tracking.AppsFlyer/Runtime/AppsFlyerBackend.cs`, `Tracking.AppsFlyer/Runtime/AppsFlyerBackendBootstrap.cs`, `Tracking.AppsFlyer/README.md`. `Packages/manifest.json` also has the new `"com.gemmob.integrationsdk.tracking.appsflyer": "file:../Tracking.AppsFlyer"` entry (uncommitted).
  - IMPORTANT CAVEAT DISCOVERED: the global `Editor.log` at `C:\Users\kiengem\AppData\Local\Unity\Editor\Editor.log` is shared across ALL Unity Editor instances on this machine (not per-project). At one point 2 `Unity.exe` processes were running simultaneously (one for IntegrationSDK, one for an unrelated game project with "Layer Lab", "StampAssets", "Epic Toon FX" assets), and both interleaved writes into the same log file, making the compile-error diagnosis unreliable — the CS0234/CS0246 errors seen (lines ~640-671) may be STALE (from before the asmdef fix) rather than current. User was asked to confirm how many Unity Editor windows are actually open; was mid-way through checking when they asked to pause and save state via "resume".
  - NEXT STEPS when resuming: (1) confirm with user only one Unity Editor (for IntegrationSDK) is running — kill/close any stray Unity.exe if needed; (2) get a CLEAN read on whether AppsFlyerBackend.cs still has compile errors (either re-check Editor.log after confirming single instance + fresh recompile, or just run Unity batchmode directly once no GUI instance holds the lock — `Temp/UnityLockfile` presence means it's locked); (3) if genuinely still erroring, re-diagnose (the asmdef fix may not have been enough, or there may be a second issue); (4) once compiling cleanly, run EditMode tests (expect still 22/22, no new tests for this task), then commit `Tracking.AppsFlyer` + `Packages/manifest.json` with message "Add Tracking.AppsFlyer package with self-registering ITrackingBackend", then run task-reviewer per the normal subagent-driven-development flow.
Task 10 (Ads.AppLovin): still BLOCKED — AppLovin MAX SDK (MaxSdk/MaxSdkCallbacks) not installed in this project; requires manual AppLovin Integration Manager GUI step by the user (different process than AppsFlyer's git-URL UPM — advised user separately). Not attempted, no files created. Not part of current resume focus (AppsFlyer is closer to done).
